.title-container {
    width: 100%;
    flex-wrap: wrap;
}

.title-label {
    width: 100%;
    height: 100%;
    font-size: 300px;
    -unity-text-align: middle-center;
    flex-wrap: wrap;
    white-space: normal;
}
